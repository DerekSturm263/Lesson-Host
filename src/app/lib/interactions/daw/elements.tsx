'use client'

import Box from '@mui/material/Box';
import { InteractionProps } from '@/app/lib/types';

type DAW = {
  placeholder: boolean
};

export default function DAW({ elementID, isDisabled, mode }: InteractionProps) {
  return (
    <Box
      sx={{ flexGrow: 1 }}
    >
      
    </Box>
  );
}
